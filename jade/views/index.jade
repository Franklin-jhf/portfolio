extends ../layout

append config
  - var metaDesc = 'I make things look good'
  - var title = 'Franklin Floresca'

block MainContent
  .loader
    .loader-dots
      span.loader-dot
      span.loader-dot
      span.loader-dot
  script.
    document.addEventListener("DOMContentLoaded", function() {
      var body = document.querySelector('body')
      var loader = document.querySelector('.loader')
      body.removeChild(loader)
    });
  #app
